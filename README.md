# 📏 PUBG Distance Calculator (Overlay Edition)

**PUBG 屏幕测距工具 - 高性能重构版**

这是一个基于 **.NET 8** 和 **ImGui** 重构的 PUBG 外部测距工具。与传统的 WinForms (GDI+) 工具不同，本项目使用 GPU 进行绘制，支持 2K/4K 分辨率，并在不使用时完全鼠标穿透，不影响游戏操作。

## ✨ 核心特性 (Features)

*   **极致性能**：使用 DirectX/OpenGL 后端渲染 (ImGui)，丝滑流畅，无闪烁。
*   **无干扰交互**：默认鼠标穿透 (Passthrough)，仅在按住热键时响应点击。
*   **高清适配**：完美支持 1080P、2K、4K 分辨率，点线位置精准无偏差。
*   **配置保存**：一次校准，永久使用。自动保存 `100m` 像素比例到本地文件。
*   **极简操作**：简单的热键组合，支持一键测距、右键重置、快速退出。

## 🛠️ 安装与运行 (Installation)

1.  在 **Releases** 页面下载最新版本的压缩包。
2.  解压所有文件到同一个文件夹。
3.  找到文件夹中的`PUBG_CalculateDistance.exe`右键单击`以管理员身份运行`。
4.  可右键`PUBG_CalculateDistance.exe`发送到桌面生成快捷方式。
5.  进入游戏，确保游戏设置为 **“无边框模式” (Borderless)**。

> **提示**：程序启动后会在屏幕左上角显示 "PUBG DistanceCalculation Running" 提示，2秒后自动隐藏。

## 🎮 使用教程 (User Guide)

### 1. 首次使用：校准 (Calibration)
*为了让工具知道你屏幕上的距离比例，首次使用需要校准一次。*

1.  在游戏中打开地图 (`M` 键)。
2.  **【关键】将地图滚轮滚到最大放大倍率**。
3.  按住 **`Ctrl`** 键，屏幕会提示 `[Calibrate] Grid Start`。
4.  **鼠标左键** 点击地图上任意一个白线网格（100m）。
5.  **鼠标左键** 点击该网格100m处。
6.  校准完成！此时按住 **`Ctrl`** 同时按一下 **`S`** 键，保存配置。

### 2. 日常测距 (Measurement)
*配置保存后，以后进游戏无需再次校准。*

1.  打开地图，**确保滚轮滚到了最大放大倍率**（需与校准时一致）。
2.  按住 **`Ctrl`** 键，屏幕提示 `Ready`。
3.  **鼠标左键** 点击你的位置。
4.  **鼠标左键** 点击敌人的位置。
5.  屏幕显示距离（例如 `345 m`）。松开 `Ctrl` 键即可继续游戏，距离显示会保留。
6.  按住`Ctrl`+`右键`将距离显示清除。

### 3. 快捷键列表 (Hotkeys)

| 快捷键 | 作用 | 说明 |
| :--- | :--- | :--- |
| **按住 Ctrl** | **激活模式** | 只有按住时才能点击画线，松开后鼠标穿透 |
| **Ctrl + S** | **保存配置** | 将当前的校准数据保存到 `calibration.txt` |
| **鼠标右键** | **重置/清除** | 测距时清除线条；连续点击可清除校准 |
| **Ctrl + `** (反引号) | **彻底退出** | 立即关闭程序 (Quit) |

## ⚠️ 重要注意事项 (Important)

1.  **关于地图缩放**：由于 PUBG 地图缩放是动态的，**请务必保证“测距时的地图放大倍率”与“校准时的放大倍率”一致**。
    *   强烈建议：每次测距前，习惯性将地图滚轮滚到最大，这样能保证 100% 准确。
2.  **关于全屏模式**：请务必使用 **“无边框 (Borderless)”** 或 **“窗口化”** 模式。全屏独占模式可能会导致覆盖层无法显示。
3.  **以管理员身份运行**：必须以管理员身份运行，否则优先级始终不如PUBG窗体。可右键打开属性令exe始终以管理员身份运行。
4.  **安全性**：本工具仅为外部屏幕覆盖层 (Overlay)，**不读取、不修改游戏内存**，理论上属于安全范围。但所有第三方辅助工具均存在潜在风险，请自行承担使用责任。

## 💻 开发构建 (Build)

*   **IDE**: Visual Studio 2022
*   **Framework**: .NET 8.0
*   **Dependencies**:
    *   `ImGui.NET`
    *   `ClickableTransparentOverlay`

**发布步骤：**
1.  选择 `Release` 模式。
2.  右键项目 -> 发布 -> 文件夹 (Folder)。
3.  配置：独立 (Self-contained), win-x64, **不要勾选单文件**。

---

**Enjoy your Chicken Dinner! 🍗**